name: Build Docker Image Manually
on:
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      

      
    - name: Build Docker images
      run: docker build . --file Dockerfile --tag mon-image:$(date +%s)
